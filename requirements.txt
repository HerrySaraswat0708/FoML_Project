# Prefer `conda env create -f environment.yml` for local development, since
# RDKit and PyTorch Geometric are generally more reliable through Conda.
#
# This file is the pip-based requirements list used by Streamlit Community
# Cloud (app.py's "Predict" tab needs torch/torch_geometric/rdkit at
# deploy time, in addition to the lightweight dashboard stack below).

joblib==1.3.2
matplotlib==3.8.4
numpy==1.26.4
pandas==2.2.2
scikit-learn==1.4.2
streamlit==1.33.0

--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.7.1
torch_geometric==2.7.0
rdkit
