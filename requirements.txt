# Prefer `conda env create -f environment.yml` for a full working setup,
# because RDKit and PyTorch Geometric are much more reliable through Conda.
#
# This file is only a lightweight pip fallback for the non-RDKit stack.

joblib==1.3.2
matplotlib==3.8.4
numpy==1.26.4
pandas==2.2.2
scikit-learn==1.4.2
streamlit==1.33.0
